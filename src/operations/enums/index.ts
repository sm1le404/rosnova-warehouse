export enum OperationStatus {
  CREATED = 'created',
  STARTED = 'started',
  PROGRESS = 'progress',
  INTERRUPTED = 'interrupted',
  STOPPED = 'stopped',
  FINISHED = 'finished',
}

export enum OperationType {
  OUTCOME = 'outcome',
  SUPPLY = 'supply',
  STRAIT = 'strait',
  RETURN = 'return',
}
