export enum EventType {
  DEFAULT = 'DEFAULT',
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}

export enum EventCollectionType {
  DEFAULT = 'DEFAULT',
  OUTCOME = 'OUTCOME',
  SUPPLY = 'SUPPLY',
  FUEL = 'FUEL',
  SHIFT = 'SHIFT',
  SETTINGS = 'SETTINGS',
}
