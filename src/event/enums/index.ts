export enum EventType {
  DEFAULT = 'default',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum EventCollectionType {
  DEFAULT = 'default',
  OPERATION = 'operation',
  FUEL = 'fuel',
  SHIFT = 'shift',
  SETTINGS = 'settings',
}
