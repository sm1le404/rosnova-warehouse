export enum TankStrelaHelperParams {
  FULL_REGISTERS = 0x2a,
  DATA_ADDR = 0x03,
  COMMAND_READ = 0x04,
  DATA = 0x00,
}

export enum TankStrelaDeviceParams {
  LAYER_FLOAT = 0,
  WEIGHT = 1,
  VOLUME = 2,
  DENSITY = 3,
  TEMP = 4,
  TOTAL_VOLUME = 13,
}
